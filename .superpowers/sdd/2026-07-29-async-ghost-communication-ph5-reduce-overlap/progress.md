# SDD ledger — plan: docs/superpowers/plans/2026-07-29-async-ghost-communication-ph5-reduce-overlap.md
## Setup
- Continues Ph0-4+T4.5 (ledger: ../2026-07-28-async-ghost-communication-ph4-latency-hiding/progress.md). Worktree /tikhome/weeber/es/.claude/worktrees/comm on worktree-comm (merged origin/python 08090fdd08, CI green). Post-merge A/B (caliper-off): 1k parity, 10k/4 +3.0%, 10k/8 +4.7% (~347µs/step abs).
- User directives this phase: (1) local transfers into halo_exchange_start (agreed: split-phase win; collective stays in finish; bitwise identical); (2) reduce/step-2 overlap (interior pass before finish); (3) ROBUST interior definition: on 1 rank interior = neighborship does NOT wrap the periodic boundary even without ghost cells.
- Builds: verify /ssd/weeber/comm-build (RelWithAssert+-Werror); timing /ssd/weeber/comm-build-nocali + reference /ssd/weeber/comm-python-nocali (@318cf1abf7). Idle gate for timing. perf now ENABLED (paranoid=0); records in /ssd/weeber/perf/{async,stag}_{1k8,10k8}.data.
## Task ledger
- PERF IDENTIFICATION (cycles:u, whole-run, skin-tune confound noted): 10k/8 async +4.2% cycles vs stag. Deltas: memset +~1.0% (CommBuf resize zero-fill — NEW Task 5.4), pack-path +~0.6% (pack_cells 6.59 vs prepare+put 6.30), MPI busy-wait LOWER for async (wait_any works; 1k/8: vader -1.4, opal_progress -1.0), diffuse ~+2% init_forces/update_cabana sweeps (skin-tuning confound; pair kernel correspondingly cheaper for async). 1k/8 parity (-0.5% cycles). Reports /tmp/rep_*.txt, data /ssd/weeber/perf/.
- USER refinements: step-2 split OMITTED at 1 MPI rank (eligibility += comm.size()>1). Plan committed 9820911f80 + 5.4 addendum. BASE_for_5.1 = <next line>
BASE_for_5.1 = 2f74f28d6525b0e9dd30a10b77e9f137a1f3bd0d
- Task 5.1: complete (commit 3b48757a62, review Approved, no issues). local_cell_copy loop moved to end of pool-overload halo_exchange_start (post-isend, outside caliper regions, after NONE guard); collective stays in finish; correctness comment committed; no test-contract changes. 149/149 + full parity gate + caliper green. BASE_for_5.2 = 3b48757a62.
- Task 5.2: complete (commits 50c874cab4 + 24fe5fdec0/5149cc3c27 fix round; re-review clean). Wrap rule = precise pair-predicate in mark_boundary_cells (first<->last layer pair on axes with node_grid==1 && periodic), supplied by RegularDecomposition; Atom/Hybrid all-boundary (documented). FIX ROUND 1 [Critical]: cell_grid[i]==1 on a wrap axis -> wrap neighbors fold to self and are excluded -> false interior; fixed by forcing ALL local cells boundary when any wrap axis has cell_grid==1 (+ degenerate RED->GREEN test). Validator check #6 = overlap-safety invariant (interior not in NeighborComm.send regions nor LocalComm.src; recv/dst are ghosts by coverage). 149/149 + parity incl. lj@1. BASE_for_5.3 = 5149cc3c27.
- Task 5.3: complete (commits 438dd545c3 + 51f629d100 fix round; review Approved, re-review clean). Split-phase reduce: CellStructure::{ghosts_reduce_forces_start,_finish,has_pending_ghost_reduce} (pending optional<GhostExchange>; asserts in resort/set_decomposition/destructor); eligibility whitelist (comm>1, no comfixed, no force_cap, NVT-VV or symplectic-Euler, no NPT, no LB-tracer; all rank-uniform args); integrate loop: interior step-2 -> finish -> boundary step-2 with shared kernel factory make_step2_particle_kernel; initial-force block drains pending; BITWISE-IDENTICAL overlap-on/off (sum pos + sum|F| equal, 200 steps, 4 ranks). FIX ROUND 1: caliper BEGIN-after-emplace/END-before-reset + ReduceGuard RAII (unwind hygiene). 149/149 + full parity + caliper green. BASE_for_5.4 = 51f629d100.
- Task 5.4: complete (commit d1c4c67715, review Approved, no issues). DefaultInitAllocator<char> on CommBuf::buf (construct(T*) default-init; reviewer verified the 0-arg overload is what allocator_traits resolves — actually suppresses value-init); bonds untouched; 8 call sites audited write-before-read; invariant documented class+method level. 149/149 + parity 4/4.
- Ph5 COMBINED A/B launching: rebuild comm-build-nocali @ d1c4c67715; lj 1k(1/8)+10k(4/8) with repeats; vs pre-Ph5 async (merged.csv @08090fdd08: 2.356e-4 / 5.888e-4 / 5.133e-3 / 7.726e-3) and vs python-staggered (pyref.csv: 2.358e-4 / 5.866e-4 / 4.986e-3 / 7.379e-3).
- ***PH5 COMBINED A/B (caliper-off, idle, 2 reps, spread <1%):*** vs pre-Ph5 async: 1k/1 +0.1% (1-rank blocking path, no cost — as designed) | 1k/8 -1.8% | 10k/4 -0.6% | 10k/8 +0.1%. vs python-staggered: 1k/1 -0.0% | 1k/8 **-1.4% (async now FASTER than staggered)** | 10k/4 +2.3% | 10k/8 +4.8% (unchanged).
- INTERPRETATION: overlap pays where waits have a latency/jitter component (1k/8) and at moderate volume (10k/4); at 10k/8 the reduce wait is pure critical-path imbalance — overlap converts fast-rank idle to work but the step ends when the slowest rank finishes, so wall time unchanged. Memset gain (5.4) mostly in warmup/growth, small in steady state. Residual 10k/8 +4.8% = serialization volume + imbalance; on-node levers exhausted; future = less-data/coalescing tradeoffs or load balancing, and multi-node where overlap+async should pay more.
- Ph5 CONCLUDING: push worktree-comm -> me:async-ghost-comm, watch CI.
- Task 5.5: complete (commits 57367622da + 50b93bf52a fix round; review Approved, re-review clean). GHOSTTRANS_QUAT/TORQUE runtime bits; quat/torque out of POSITION/FORCE arms; whitelist System::orientation_ghosts_needed() (rot propagation / vs_relative / dipolar solver / Gay-Berne / LB+ENGINE / SW-Langevin arm from fix round); reduce paths via get_force_reduce_ghost_flags(); add_forces data_parts-aware; op-sanity + Add dispatch accept FORCE|TORQUE. REAL BUG FOUND: vs_relative_update_particles read ghost quat relying on implicit quat-with-POSITION — now requests DATA_PART_QUAT explicitly (was ~0.2% force error at 2+ ranks once split). Payload for LJ: position push 68->36B (-47%), reduce 48->24B (-50%). 149/149 + parity + rotation/dipolar/vs_relative coverage green.
- 5.5 A/B launching: rebuild nocali @50b93bf52a; lj 1k(1/8)+10k(4/8) 2 reps; vs Ph5-pre-5.5 (ph5.csv: 2.358e-4 / 5.784e-4 / 5.101e-3 / 7.732e-3 means) and vs python-staggered (2.358e-4 / 5.866e-4 / 4.986e-3 / 7.379e-3).
- T5.5 A/B RESULT (volume hypothesis FALSIFIED on-node): halving payload => +0.1% everywhere (1k/8 -1.3% vs stag holds; 10k/8 +4.9% unchanged). Together with 5.4-memset also flat: on-node wall time INSENSITIVE to ghost serialization cost — pack is per-particle-overhead bound (~0.4GB/s effective) AND savings land off the critical path (slowest rank's pair loop sets step time; 15% min-max pair-loop spread). Lever B (specialized pack loops) NOT pursued — same off-critical-path fate predicted. 5.5 KEPT: correct, vs_relative bug fix, halved wire volume = multi-node value.
- ***Ph5+T5.5 CONCLUSION:*** on-node levers exhausted; remaining 10k/8 +4.9% = load-imbalance critical path (tree-based LB is the planned future fix) + engine cycle overheads off-path. Async engine vs staggered final on-node standing: 1k parity-to-better (-1.3% @8r), 10k/4 +2.4%, 10k/8 +4.9%. Pushing @50b93bf52a.
## PR 5404 CI (round 2) ledger
- Fixed round 1 (pushed 7ea9cf3c8c): AppleClang unused-lambda-capture (wrap_axis by value) + 12 unresolvable doxygen @ref->@c.
- ***REAL ENGINE BUG (Ph1-era) FOUND VIA icc@3 (commit 3c62b1353e + regression test 2d0fb8ae3c):*** LocalComm self-copies never role-swapped for Reduce -> self-wrap ghost forces (node_grid==1 axes) silently dropped. Masked everywhere by unique_particles dedup in the Cabana SR path; exposed only by ICC's legacy-style accumulate+reduce at 3 ranks (cell_grid_x==1, distinct peers). Deviation deterministic 1.2537e-4 (fixed-point bias, convergence-sweep-proven). Fix: role-swap (dst->src, ADD) on Reduce in halo_exchange_start; reviewer Approved; RED->GREEN regression test pinned. icc@2/3/4 pass; probes bitwise; parity incl lj@3 pass.
- KNOWN LATENT (documented, not fixed): make_halo_plan leaves tangential image_box shift {} on corner/edge self-wrap ghosts (inert for min-image + local P3M; matters only for future unfolded-ghost-position consumers).
- coulomb_interface@3 tuning failure + {1,1,3} icc failure = PRE-EXISTING (python-HEAD fails identically) — not ours; mention on PR.
- VERIFICATION HOLE closed: comm-build had ADDITIONAL_CHECKS OFF the whole time (validator never ran locally). Rebuilding with maxset (checks ON) to reproduce+fix the CI validator asserts (dozens of python tests on fedora/macos/GitLab).
- Task CI-r2 validator fix (commit 581dc1629b): check #6 ("interior cell in NeighborComm send / LocalComm src") fired on Lees-Edwards fully_connected_boundary whole-plane regions and ELC periodicity-change paths — cells geometric rules never see. Fix: add GhostComm::mark_plan_cells_boundary(HaloPlan const &, local_cells) in mark_boundary_cells.hpp (reverse-map ParticleList*->Cell*, iterate send regions and LocalComm.src, mark boundary); call it in RegularDecomposition after the two existing geometric passes. Atom/Hybrid unaffected (force-all-boundary already). Evidence: regular_decomposition@4 OK (was order-dependent abort), coulomb_interface@4 OK, full 20-test broad sweep @4 OK, lj@1/2/3/4 OK, lees_edwards@4 OK, nsquare@4 OK, hybrid_decomposition@4 OK, 149/149 unit tests OK, format+clang clean.
- CI ROUND 2 PUSHED (7ea9cf3c8c..e839e3652d = icc engine fix 3c62b1353e + regression test 2d0fb8ae3c + plan-derived boundary marking / diagnostic asserts e839e3652d). Local gate on checks-ON build: 149/149; regular_decomposition@4, coulomb_interface@4, icc@3, lj@3, lees_edwards@4 all clean, zero violations. Broad 20-test sweep green (fix agent). Awaiting PR 5404 checks (poll bn5kp6kl5). PRE-EXISTING failures to mention on PR (NOT ours, python-HEAD fails identically): coulomb_interface@3 P3M "failed to reach requested accuracy"; icc @ node_grid {1,1,3} same tuning error.
- Task CI-r3: complete (commit 430591b83b). Root cause: validate_halo_plan_symmetry (collective all_to_all) in decomposition ctors fired during checkpoint load while maximal_cutoff was transiently rank-divergent (rank0 cell_grid {2,1,7} vs ranks1-3 {1,1,4}); plan never used before consistent rebuild, so asymmetry was harmless, but ctor-time collective + one-rank abort caused MPICH to kill the whole batch. Fix: remove symmetry assert from all three decomposition ctors (replace with explanatory comment); add mutable bool symmetry_validated = false to HaloPlan; run once-per-plan validate_halo_plan_symmetry + GhostComm::report_violations at the top of halo_exchange_start (pool overload, ESPRESSO_ADDITIONAL_CHECKS, after GHOSTTRANS_NONE guard). All ranks enter halo_exchange_start together so the all_to_all is safe there; flag rearmed naturally on every plan rebuild. 149/149 unit tests; checkpoint__therm_lb__p3m_cpu__lj__lb_walberla_cpu_ascii 2/2, all 5 other CI-failing checkpoint variants 10/10; sweep re-check (regular_decomposition@4, coulomb_interface@4, icc@3, lj@3) exit 0, zero halo-plan-validation lines. Format + -Werror clean.
